# Replicate Generate

Generates AI images using Replicate's Google Nano Banana 2 model with support for various resolutions (512px-4K), aspect ratios, and output formats (URL or local file).

## Usage

This tool is available as `replicate_generate` in the EloPhanto agent.

## Parameters

See `schema.json` for the full input schema.

## Created

Auto-generated by EloPhanto's self-development pipeline on 2026-03-06.
